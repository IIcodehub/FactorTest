# 数据处理核心库
pandas>=1.3.0
numpy>=1.20.0

# 高性能文件读写 (Parquet支持)
pyarrow>=6.0.0

# Excel 文件读取 (读取因子字典和月份状态表)
openpyxl>=3.0.0

# 数据库连接 (SQL Server)
pymssql>=2.2.0

# 科学计算与统计 (IC计算, Newey-West调整)
scipy>=1.7.0
statsmodels>=0.13.0

# 绘图与可视化
matplotlib>=3.4.0

# PDF 报告生成与合并
reportlab>=3.6.0
PyPDF2>=2.0.0

# 进度条显示
tqdm>=4.60.0